# Sealed method versions

Each `vX.Y.Z.json` is a **seal**: the technique ids and commitment summary programmes should pin.

| File | Notes |
|------|-------|
| [`v0.1.0.json`](v0.1.0.json) | First seal — core commitments via T-001 |

Living tip (`METHOD.md` on `main`) may advance ahead of the latest seal. Production programmes pin seals, not arbitrary commits, unless intentionally tracking development.
